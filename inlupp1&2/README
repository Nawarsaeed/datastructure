In order to run all programs, you need to do the following steps

	1)First, navigate to the build folder using (cd build).

	2)Now you have to type the command (camke ..).
	3)last step, type command (make).

Now all programs are in the build folder. To run a program, type (./the_name_of_the_program). For exampel first Exercise 1, type (./task1).


